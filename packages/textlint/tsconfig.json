{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "baseUrl": "../packages",
    "paths": {
      // TODO: re-structure directry
      // Move textlint-kernel to @textlint/kernel
      "@textlint/kernel": [
        "./textlint-kernel/src"
      ]
    },
    "outDir": "./lib/"
  },
  "include": [
    "src/**/*"
  ],
  "exclude": [
    ".git",
    "node_modules"
  ]
}
