{
  "private": true,
  "scripts" : {
    "build": "textlint-scripts build",
    "test": "textlint-scripts test",
    "ci": "npm rm textlint-scripts && npm i && npm run build && npm run test"
  },
  "devDependencies": {
    "textlint-scripts": "file:../"
  }
}
