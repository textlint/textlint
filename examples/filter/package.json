{
  "name": "filter-comments",
  "version": "1.0.0",
  "description": "filter rule example",
  "main": "index.js",
  "scripts": {
    "test:ci": "rm -rf ./node_modules && yarn --mutex file && npm test",
    "test": "npm run textlint",
    "textlint": "textlint -f pretty-error README.md"
  },
  "author": "azu",
  "license": "MIT",
  "devDependencies": {
    "textlint": "file:../../",
    "textlint-filter-rule-comments": "^1.2.0",
    "textlint-rule-no-todo": "^2.0.0"
  }
}
