{
  "name": "fix",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:ci": "rm -rf ./node_modules && yarn-install && npm test",
    "test": "npm run textlint && npm run textlint-fix",
    "textlint": "textlint README.md",
    "textlint-fix": "textlint --fix README.md"
  },
  "author": "azu",
  "license": "MIT",
  "devDependencies": {
    "textlint": "file:../..",
    "textlint-rule-prh": "^2.4.0"
  }
}
