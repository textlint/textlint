{
  "name": "cli",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:ci": "npm rm textlint && npm i --no-progress && npm test",
    "test": "npm run textlint",
    "textlint": "textlint --rulesdir ./rules README.md"

  },
  "author": "azu",
  "license": "MIT",
  "devDependencies": {
    "textlint": "file:../.."
  }
}
