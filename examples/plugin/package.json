{
  "name": "plugin",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:ci": "rm -rf ./node_modules && yarn && npm test",
    "test": "npm run textlint",
    "textlint": "textlint -f pretty-error README.md"
  },
  "author": "azu",
  "license": "MIT",
  "devDependencies": {
    "textlint": "file:../.."
  },
  "dependencies": {
    "textlint-plugin-jtf-style": "^0.1.5"
  }
}
